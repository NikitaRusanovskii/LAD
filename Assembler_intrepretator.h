#pragma once

/*void intreptretator(grid, instr_amount, int array) {
	for (size_t i = 0; i < instr_amount; i++) {
		execute_math_command(grid, i, array);
	}
}


void execute_math_command(string** grid, size_t &iptr, int* &array) {
	size_t A1 = (size_t)grid[i][2];
	size_t A2 = (size_t)grid[i][3];	
	size_t A3 = (size_t)grid[i][4];
	if (grid[i][1] == "ADD") {
		array[R3] = array[R1] + array[R2];
	}
	if (je) {
		i=A1
	}

}*/



